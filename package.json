{
  "name": "usul",
  "version": "0.1.0",
  "description": "The strength of the base of the pillar",
  "scripts": {
    "build": "cd build && cmake --build .",
    "clear": "node_modules/.bin/clear",
    "config": "del-cli build && make-dir build && cd build && cmake .. -DCMAKE_VERBOSE_MAKEFILE=ON",
    "rebuild": "npm run config && npm run build",
    "test": "cd build/bin && usul_test --abort --use-colour=yes --durations=no",
    "test-win": "cd build/bin/Debug && usul_test --abort --use-colour=yes --durations=no",
    "watch": "watch \"npm run clear && npm run build && npm run test\" ./ --filter=./watch_filter.js",
    "watch-win": "watch \"npm run clear && npm run build && npm run test-win\" ./ --filter=./watch_filter.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/perryiv/usul.git"
  },
  "author": "Perry L Miller IV",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/perryiv/usul/issues"
  },
  "homepage": "https://github.com/perryiv/usul#readme",
  "devDependencies": {
    "clear-cli": "^1.0.1",
    "del-cli": "^1.1.0",
    "make-dir-cli": "^1.0.0",
    "watch": "^1.0.2"
  }
}
